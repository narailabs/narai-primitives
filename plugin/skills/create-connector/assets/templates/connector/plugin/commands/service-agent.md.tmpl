---
description: Run a {{SERVICE_SLUG}} action via the {{SERVICE_SLUG}}-agent connector
argument-hint: "<action> <params-json>"
---

Invoke the `{{SERVICE_SLUG}}-agent` skill with the user's $ARGUMENTS as the action name and params JSON. Return the connector's JSON envelope verbatim.
