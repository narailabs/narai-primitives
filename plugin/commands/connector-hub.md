---
description: Plan and dispatch read-only calls across enabled connectors via the connector-hub
argument-hint: "<natural-language prompt>"
---

Invoke the `connector-hub` skill with the user's $ARGUMENTS as the prompt. Return the hub's JSON envelope verbatim.
