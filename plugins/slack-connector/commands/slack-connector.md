---
description: Run a Slack action via the slack-connector connector
argument-hint: "<action> <params-json>"
---

Invoke the `slack-connector` skill with the user's $ARGUMENTS as the action name and params JSON. Return the connector's JSON envelope verbatim.
