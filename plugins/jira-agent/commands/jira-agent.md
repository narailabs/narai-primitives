---
description: Run a read-only Jira query via the jira-agent connector
argument-hint: "<action> <params-json>"
---

Invoke the `jira-agent` skill with the user's $ARGUMENTS as the action name and params JSON. Return the connector's JSON envelope verbatim.
