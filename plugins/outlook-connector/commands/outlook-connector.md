---
description: Run an Outlook (Microsoft 365 email) action via the outlook-connector connector
argument-hint: "<action> <params-json>"
---

Invoke the `outlook-connector` skill with the user's $ARGUMENTS as the action name and params JSON. Return the connector's JSON envelope verbatim.
