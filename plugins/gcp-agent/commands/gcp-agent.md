---
description: Run a read-only GCP query via the gcp-agent connector
argument-hint: "<action> <params-json>"
---

Invoke the `gcp-agent` skill with the user's $ARGUMENTS as the action name and params JSON. Return the connector's JSON envelope verbatim.
