---
description: Run a Linear query or mutation via the linear-agent connector
argument-hint: "<action> <params-json>"
---

Invoke the `linear-agent` skill with the user's $ARGUMENTS as the action name and params JSON. Return the connector's JSON envelope verbatim.
