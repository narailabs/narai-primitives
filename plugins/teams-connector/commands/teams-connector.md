---
description: Run a Microsoft Teams action via the teams-connector connector
argument-hint: "<action> <params-json>"
---

Invoke the `teams-connector` skill with the user's $ARGUMENTS as the action name and params JSON. Return the connector's JSON envelope verbatim.
